const Header = ({ title }) => {
  return (
    <section>
      {/* page-title */}
      <h2 className="strong">
        {/* {title} */}
        Header Title
      </h2>
      <section>
        {/* notification and avatar with onlin offline status */}
      </section>
    </section>
  );
};

export default Header;
